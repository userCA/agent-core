# 2026-06-10 开发日志

## 问题 74. OpenViking 适配器 live API 验证 + 记忆系统全局优化

**涉及文件：**
- `agent_core/memory/adapters/openviking_adapter.py` — 修复 3 个 bug + 统一错误处理
- `agent_core/memory/adapters/mem0_adapter.py` — 统一错误处理，异常传播
- `agent_core/memory/adapters/inmemory.py` — 添加 asyncio.Lock
- `agent_core/memory/extension.py` — 错误处理 + signal 支持 + buffer 限制
- `tests/memory/test_openviking_live.py` — 新增，实际 API 端到端测试
- `tests/memory/test_openviking_verify.py` — 新增，修复验证测试
- `tests/memory/test_openviking_store.py` — 适配异常传播行为变更
- `tests/memory/test_mem0_store.py` — 同上

**变更类型：** bug 修复 + 架构优化

**关联规则：** dev-process-backend（静默异常）、dev-process-optimizer（跨 adapter 一致性）

### 修复的 bug

#### Bug 1: `forget` 静默吞 PermissionDeniedError
- **原因：** `except Exception: logger.warning(...)` 吞掉所有异常
- **修复：** 移除 try/except，异常直接传播
- **影响：** 调用方不知道删除失败，认为操作成功

#### Bug 2: 空 `session_id` 导致 `_resolve_api_key` 崩溃
- **原因：** `PurePosixPath("").parts[0]` 抛出 `IndexError`
- **修复：** 加 `if "/" in session_id` 守卫，不包含斜杠则直接回退默认 key

#### Bug 3: URL `/api/v1` 重复前缀
- **原因：** SDK 内部在所有路径前拼接 `/api/v1/...`，用户传入带 `/api/v1` 的代理 URL 会产生 `/api/v1/api/v1/...`
- **修复：** `url.removesuffix("/api/v1")` 自动去除

### 架构优化

#### P0: 统一 adapter 错误处理策略
- 全部 3 个 adapter（InMemory/Mem0/OpenViking）的所有方法（remember/recall/forget）统一为：异常直接抛出
- 移除不再使用的 `import logging` + `logger`
- 影响：`MemoryExtension` 需要自己处理异常（见下）

#### P1: MemoryExtension 错误处理
- `remember` 失败：单个失败不影响其他 pending，日志记录
- `recall` 失败：回退返回原始 `llm_messages`，不阻断对话
- `_pending` 上限 `_MAX_PENDING=100` 防止无界增长
- `transform_context` 中检查 `signal.is_set()` 支持取消
- 空 query 提前返回避免无效 API 调用

#### P2: InMemoryMemoryStore 异步安全
- 添加 `asyncio.Lock` 保护所有读写操作

### 已知限制

- OpenViking `recall` 使用 `find()` 搜索 `viking://user/memories`，返回的是记忆结构文档（.abstract.md/.overview.md），而非原始用户事实。这是 OpenViking `commit_session` 记忆提取管线的设计特性，不是 adapter bug。`search()` API 是 experimental 且当前服务版本不稳定。
- OpenViking `forget` 调用 `delete_session`，在共享 session 服务上需要 ADMIN/ROOT 权限

## 问题 75. H5 侧边栏菜单 + 设置页优化 + 咪兔头像

**涉及文件：**
- `scene/http_sse/static/src/components/shared/Icon.tsx` — 新增 menu、user、close 图标
- `scene/http_sse/static/src/components/input/ChatInput.css` / `.tsx` — H5 输入组件适配
- `scene/http_sse/static/src/h5/App.css` / `.tsx` — 容器 position: relative + 主题变量
- `scene/http_sse/static/src/h5/components/CompactHeader.css` / `.tsx` — 菜单侧边栏 + 毛玻璃 + 主题切换
- `scene/http_sse/static/src/h5/components/HistoryPage.css` / `.tsx` — 历史记录页样式微调
- `scene/http_sse/static/src/h5/components/LoginPage.tsx` — 登录页微调
- `scene/http_sse/static/src/h5/components/AuthPanel.css` — 弹窗 max-width: 430px
- `scene/http_sse/static/src/h5/components/SettingsPage.css`（删除）/ `.tsx` — 内联样式重构 + 咪兔 SVG 头像
- `scene/http_sse/static/src/h5/components/BottomTabBar.css` — 底部标签栏样式
- `scene/http_sse/static/src/h5/components/H5ChatInput.css` / `.tsx`（新增）
- `scene/http_sse/static/src/h5/theme/h5.css` — CSS 变量系统完善

**变更类型：** 功能增强 + UI 优化

**关联规则：** dev-process-frontend（H5 移动端布局）

### 功能清单

1. **侧边栏菜单**：Header 左侧添加汉堡菜单，收纳宠物资料、会话历史、设置
   - `position: absolute` 定位（相对 `.h5-app-layout`），避免超出 H5 容器
   - `backdrop-filter: blur(20px)` 毛玻璃效果，不遮挡聊天内容
   - `max-width: 70vw`，宽度 200px
   - motion AnimatePresence + spring 动画
   - 点击外部/Escape 关闭

2. **主题切换移至 Header 右上角**：从菜单移入 header actions 区域

3. **设置页面优化**：
   - 移除主题切换项（已在 Header）
   - 统一图标尺寸（28px）、字体（14px）、间距（10px）
   - Profile Header 添加咪兔风格 SVG 猫脸头像（SVG 绘制）

4. **认证弹窗适配**：`.h5-auth-panel` 添加 `max-width: 430px` 防止超出屏幕

5. **ChatInput 组件**：提取为 H5 专用版本 `H5ChatInput`，优化输入体验
