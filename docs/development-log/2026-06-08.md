# 2026-06-08 开发日志

## 70. Motion 动画库集成（5 个目标）

**类型**: 功能 + 重构 | **涉及文件**: 12+

- `package.json`：新增 `motion` 依赖（~20KB tree-shaken）
- `App.tsx`：`MotionConfig reducedMotion="user"` 全局包裹
- `HeaderCompanion.tsx`：气泡 `AnimatePresence` + `motion.span` 替代 CSS `@keyframes bubbleIn`，退场动画不再瞬移
- `MiniGameHost.tsx`：停靠面板 spring 展开/折叠 + 结果药丸 fade 退场，`transformTemplate` 合并 CSS 定位 transform
- `MessageBubble.tsx`：全部 4 种消息类型 `motion.div` + `layout` prop，新消息 fade+slideUp 入场
- `CompanionSprite.tsx`：`motion.pre` 替代 `<pre>`，情绪变化 `useAnimate` pulse，浮动动画迁移到 Motion
- `Header.css`：删除 `@keyframes bubbleIn`、`@keyframes headerCompanionFloat`
- `FishingGame.css`：删除 `@keyframes resultFadeIn`、3 处局部 `@media (prefers-reduced-motion)`
- `LoginCompanion.css`：删除 2 处局部 `@media (prefers-reduced-motion)`
- CSS 定位 transform（`translateX(-24%)`/`translateX(-50%)`）与 Motion 内联 transform 冲突，通过 `transformTemplate` 合并解决

## 71. 小游戏系统完整移除

**类型**: 删除 | **涉及文件**: 15

- **前端删除 7 文件**：`minigames/` 目录（MiniGameHost、FishingGame、CatchGame、TapGame、game-api、types、FishingGame.css）
- **前端修改 4 文件**：Header.tsx（移除 import+渲染）、HeaderCompanion.tsx（移除 ResizeObserver 定位逻辑，-35 行）、Header.css（移除 `--minigame-chip-left`）、companion-store.ts（更新注释）
- **后端删除 2 端点**：`GET /api/minigame/config`、`POST /api/minigame/feed`（server.py -90 行）
- **后端删除 1 包**：`agent_core/companion/minigames/`（6 个 .py 文件）
- CSS 66.8KB（-6KB）、JS 468KB（-12KB）

## 72. UX 审计：touch targets + 对比度 + touch-action

**类型**: UX | **涉及文件**: 3

- `Header.css`：伴侣气泡 `color: var(--mute)` → `var(--body)`，对比度 3.8→5.0:1
- `tokens.css`：`.btn` 添加 `min-height: 44px`
- `ChatInput.css`：`.action-btn`/`.send-btn`/`.input-clear-btn` 尺寸从 `width/height: 20/36px` → `min-width/min-height: 44px`；`.input-clear-btn` 补上 `touch-action: manipulation`

## 追加：Motion + SVG 第二轮优化

| 文件 | 变更 | 类型 |
|------|------|------|
| `App.tsx` | 页面切换 `AnimatePresence mode="wait"` + motion.div，AuthModal 去除外部条件渲染 | 增强 |
| `Pages.css` | 移除旧 CSS `pageIn`/`pageOut` keyframes，新增 `.page-wrapper` flex 布局 | 重构 |
| `ConfirmDialog.tsx` | `AnimatePresence` + motion.div overlay/dialog spring 弹入 | 增强 |
| `AuthModal.tsx` | 自管理 authModalOpen + AnimatePresence spring 弹入 | 增强 |
| `Sidebar.tsx` | 合并双 aside → 单 `motion.aside layout` + AnimatePresence 内容淡入淡出 | 重构 |
| `Sidebar.css` | 新增 `.sidebar-expanded-inner` 修复滚动容器约束链断裂 | 修复 |
| `Header.tsx` | 主题切换图标 AnimatePresence rotate + scale 过渡 | 增强 |
