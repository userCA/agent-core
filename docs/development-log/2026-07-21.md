# 2026-07-21 开发日志

## C3：Prompt 预算预检 MVP

**类型:** 增强 / Context 工程  
**关联:** `docs/agent-engineering-optimization.md` C3

### 变更摘要

| 区域 | 文件 | 变更 |
|------|------|------|
| 库 | `compaction/budget.py` | `estimate_prompt_tokens` / `is_prompt_budget_exceeded` / `PROMPT_BUDGET_EXCEEDED` |
| Core | `core/context.py` | `AgentLoopConfig.prompt_budget_ratio`（默认 0.95；`None` 关闭） |
| Core | `core/loop.py` | turn 前预检 → 尝试 compact 一次 → 仍超则错误助手消息且**不调 provider** |
| 测试 | `tests/compaction/test_prompt_budget.py` | 超限不调 provider；compact 后继续 |

### 关闭

```python
AgentLoopConfig(..., prompt_budget_ratio=None)
```

### 注意

- 默认 `prompt_budget_ratio=0.95`：**行为变更**——超预算会在调 provider 前失败（先尝试 compact）
- `max_output_tokens` 过大时会钳制预留，避免 limit 塌成 1 误拦短对话

---

## C2：L3 Compaction 强化 + Scene 默认接线

**类型:** 增强 / Context 工程  
**关联:** `docs/agent-engineering-optimization.md` C2

### 变更摘要

| 区域 | 文件 | 变更 |
|------|------|------|
| 库 | `compaction/cut_point.py` | `find_safe_cutoff`：不从 `tool_result` 起切；min_keep/min_delete |
| 库 | `compaction/summarize.py` | 确定性结构化 handoff（请求 / 历史 / artifact ref 索引） |
| 库 | `compaction/compactor.py` | 使用 cut-point；向 summarize_fn 传 `instructions`；`create_default_compactor()` |
| Scene | `http_sse` / `h5` `compaction_config.py` + `chat_assistant` | `ENABLE_COMPACTION=1` 默认注入 compactor |
| 测试 | `tests/compaction/test_cut_point_and_summary.py` | cut-point / handoff / instructions |

### 说明

- MVP 使用**确定性**摘要（无额外 LLM 调用）；后续可用 LLM `summarize_fn` 替换
- 关闭：`ENABLE_COMPACTION=0`

---

## C1 MVP：L1 ToolResultRefStore（artifact 外置）

**类型:** 新功能 / Context 工程  
**关联:** `docs/agent-engineering-optimization.md` C1、`docs/reference/agent_engineering.md` §3.2 L1

### 变更摘要

| 区域 | 文件 | 变更 |
|------|------|------|
| 库 | `agent_core/artifacts/*` | `ArtifactStore` / `InMemoryArtifactStore`、`ArtifactExternalizeExtension`、`create_artifact_extension` |
| Core | `core/messages.py` | `ToolResultMessage.details` 可选字段，承载 `__refId` 等元数据 |
| Core | `core/tool_runner.py` | 写入 details；after_hook 同时支持 dict / ToolResult 返回值 |
| Session | `session/persistence.py` | 持久化 tool_result 时保留 details |
| 测试 | `tests/artifacts/*` | store 往返、阈值外置、Harness e2e（LLM 只见 ref） |

### 用法（可选 Extension，Scene 默认未开）

```python
from agent_core.artifacts import create_artifact_extension

store, ext = create_artifact_extension(char_threshold=8000, summary_chars=2000)
harness = AgentHarness(..., extensions=[ext])
```

### 边界（MVP）

- 仅字符阈值；无数组元素阈值、无 L4 取回工具、无 bash 路径收敛
- InMemory：进程重启后全文丢失（ref 文案仍在 transcript）
- 外置时 **合并** 原有 `details` 顶层键（`exit_code` / `plan` / `urls` 等），不包进 `original_details`
