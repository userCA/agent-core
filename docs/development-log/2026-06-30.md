# 2026-06-30 开发日志

## 本日工作：H5 前端重构 — 参照高保真原型全面改版

### 变更类型：重构 / UI 重设计

### 摘要

参照 `scene/h5/design/oriental-chat-journal.html` 高保真原型，对 H5 前端进行 7 项结构性重构：Header 三段式布局、方向感知屏幕滑动、消息气泡带头像+时间戳+日期分割线、Agent Trace 可折叠推理卡片、输入区简化为 + | textarea | 发送、inkwash-overrides CSS 清理。同时删除 7 个已废弃的死代码文件。

### Task 1: Header 三段式重构

| 变更 | 文件 | 说明 |
|------|------|------|
| 重写 | `h5/components/CompactHeader.tsx` | 移除侧边菜单 Dialog，改为 book-open \| 居中标题+在线状态 \| 宠物像素头像三段式 |
| 重写 | `h5/components/CompactHeader.css` | 新增 `.h5-header` `.h5-header-btn` `.h5-header-center` `.h5-header-pet` 等样式 |
| 清理 | `h5/theme/inkwash-overrides.css` | 移除旧 header/menu panel 覆盖样式 |

### Task 2: Screen 导航系统

| 变更 | 文件 | 说明 |
|------|------|------|
| 重写 | `h5/App.tsx` | `TAB_ORDER` 数组 + `tabIdx()` 方向计算 + `useRef` 追踪前 tab，AnimatePresence direction-aware tween 滑动 |

### Task 3: 消息气泡布局

| 变更 | 文件 | 说明 |
|------|------|------|
| 重写 | `components/chat/MessageBubble.tsx` `.css` | flex row 布局 + MoonAvatar（月牙 SVG）+ `formatTime()` 时间戳 |
| 重写 | `components/chat/ChatContainer.tsx` `.css` | 新增 `DateDivider` 组件 + `dateLabel()` + `isDifferentDay()` 日期分割线逻辑 |
| 重写 | `components/chat/StreamingMessage.tsx` `.css` | 流式消息同样使用 MoonAvatar + row 布局 |

### Task 4: Agent Trace 卡片

| 变更 | 文件 | 说明 |
|------|------|------|
| 新增 | `components/chat/TraceCard.tsx` | 可折叠推理线程，彩色节点时间轴（think=紫/tool=蓝/skill=绿），运行自动展开、完成自动折叠 |
| 新增 | `components/chat/TraceCard.css` | `.trace` `.trace-head` `.trace-rail` `.t-step` `.t-node` `.t-body` 全套样式 |
| 替换 | `MessageBubble.tsx` `StreamingMessage.tsx` | `BlocksRenderer` → `TraceCard`，移除 `StatusBar` 组件 |

### Task 5: 输入区简化

| 变更 | 文件 | 说明 |
|------|------|------|
| 重写 | `h5/components/H5ChatInput.tsx` | 移除语音模式/上传/快捷标签，改为 + 按钮 \| textarea（auto-resize）\| 发送按钮 |
| 重写 | `h5/components/H5ChatInput.css` | 从 577 行精简至 108 行，`.h5-input-dock` `.h5-input-row` `.h5-input-textarea` |

### Task 6: inkwash-overrides CSS 清理

| 变更 | 文件 | 说明 |
|------|------|------|
| 清理 | `h5/theme/inkwash-overrides.css` | 移除 `.h5-input-dock` `.input-box` `.h5-quick-tag` `.h5-tab-bar` `.h5-input-expanded` `.h5-input-fab` `.send-btn` 等废弃样式（~84 行） |

### 死代码清理

| 操作 | 文件 | 原因 |
|------|------|------|
| 删除 | `components/chat/BlocksRenderer.tsx` | 被 TraceCard 替代，无引用 |
| 删除 | `h5/components/CollapsibleInput.tsx` + `.css` | 被 H5ChatInput 替代，无引用 |
| 删除 | `h5/components/BottomTabBar.tsx` + `.css` | 导航移至 CompactHeader，无引用 |
| 删除 | `components/input/ChatInput.tsx` + `.css` | 仅 CollapsibleInput 引用（已死） |

### 构建验证

```bash
cd scene/h5/static && npx tsc --noEmit && npx vite build
# tsc: 零错误
# vite build: 零错误，产物 App-*.js 428KB / App-*.css 82KB
```
