# 2026-05-28

## #1 修复 SSE 流式输出被 agent_loop 缓冲的问题

**问题**：用户发送消息后前端长时间只显示 thinking dots，随后思考内容和模型结果一次性全部刷出，无流式体验。

**根因**：`agent_core/core/loop.py` 的重试机制中，`_stream_assistant` 产出的所有 `MessageUpdate`（`ThinkingDelta`、`TextDelta`）被收集到 `buffered` 列表，直到整个 LLM 流结束后才一次性 `yield`。这是为了支持失败重试时不让客户端看到残片输出，但以牺牲 100% 正常情况下的流式体验为代价。

**修复**：`loop.py` 中区分首次尝试与重试尝试：
- `retry_count == 0`：直接 `yield MessageStart` + `async for ... yield upd`，实时流式
- `retry_count > 0`：保持原有缓冲逻辑，避免客户端看到失败尝试的残片

**权衡**：若首次尝试失败且触发重试，客户端可能已看到部分输出。但重试是罕见场景（网络错误/限流），为极端情况牺牲常规交互的流式体验不合理。

**验证**：全部 244 个测试通过，包括重试相关测试。

## #2 前端 UI/UX 动效与交互优化

**目标**：不偏离原始风格，增强动效和交互体验。

**改动**：
- `WelcomeScreen.css`：logo 添加 `logoFloat` 浮动动画（4s 周期，上下 6px）；example chips 添加 `chipStagger` 交错入场（scale + translateY，0.15s 间隔）
- `ChatContainer.css`：消息入场动画按角色区分 — `msg-user` 从右侧滑入（translateX + scale），`msg-assistant` 从左侧滑入；使用 `cubic-bezier(0.22, 1, 0.36, 1)` 更柔和的缓动
- `StreamingMessage.css`：思考指示器升级为 `dotWave`（垂直波动 + opacity，更优雅的三阶延迟）；光标增加 `box-shadow` 发光效果，使用 `cubic-bezier` 缓动，高度改为 `1.1em` 更自然
- `ChatInput.css`：聚焦光晕从 `2px / 0.2 opacity` 改为 `3px / 0.12 opacity` 更柔和；发送按钮添加 `hover` 微缩放（1.03）和 `active` 按压（0.97）
- `Header.css`：running 状态 pulse 升级为 `pulseRing`（scale + 扩散阴影，1.8s 周期）
- `StepsPanel.tsx` + `StepsPanel.css`：展开/收起从条件渲染（display 切换）改为始终在 DOM 中，通过 `max-height` + `opacity` + `padding` 过渡实现平滑展开
- `useAutoScroll.ts`：
  - 添加 `NEAR_BOTTOM_THRESHOLD`（80px），仅在用户接近底部时才自动滚动
  - 添加 40ms debounce，将快速连续的 deps 变化合并为单次滚动
  - 清理函数完善，避免内存泄漏

## #3 修复 StepsPanel 中间过程折叠问题

**问题**：流式输出时，StepsPanel 中思考内容和工具调用一直处于折叠态，用户无法看到当前正在输出的具体内容。

**根因**：`StepsPanel.tsx` 的 `getAutoExpandedId` 中，当 `currentText.length > 0` 时返回 `null`，导致所有 step 被收起。文本输出开始后运行中的 step 也被折叠。

**修复**：移除 `currentText` 依赖，改为始终展开 `status === 'running'` 的 step；无运行中 step 时展开最后一个。

```typescript
// 修复前
if (currentText.length > 0) return null;  // 文本输出开始后全部折叠

// 修复后
const running = steps.find((s) => s.status === 'running');
if (running) return running.id;
```

**验证**：前端构建通过，全部 244 个测试通过。
