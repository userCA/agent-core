# 2026-07-31 开发日志

## 短剧流水线连贯性优化（P0–P2）

按 [short-drama-continuity-optimization-plan.md](../short-drama-continuity-optimization-plan.md) 落地：

### P0 跨镜桥接

- 新增 `agent_core/tools/video_frame.py`：ffmpeg 抽末帧 + 咪咕远端上传
- `create_short_drama` 改为**逐镜串行**：静帧 → 视频 → 抽末帧 → 下一镜
- 镜 N>1 静帧参考「人物锚点 + 上一镜末帧 URL」
- Agnes 桥接方案：**C**（静帧链 + 单图 i2v，未启用 keyframes API）

### P1 分镜契约

- `shots[]` 扩展：`continues_from`、`transition`、`end_pose`、`camera`、`scene_id`
- 工具内组装 still/motion prompt；`continues_from` 校验
- `prompt_guidelines` 要求 LLM 拆镜时填写连贯字段

### P2 拼接与重跑

- `concat_videos` / `concat_video_files_crossfade`：支持 `transition=crossfade`、`boundary_transitions`
- `create_short_drama` 新增 `resume_shots` + `rerun_shot_ids` 单镜重跑

### 测试

- `tests/tools/test_video_frame.py`
- `tests/tools/test_short_drama_pipeline.py` 扩展桥接/重跑用例
- 相关 12 项测试通过

## P3 一致性与体验（不含 BGM）

- `scenes[]`：场景锚点 + HITL（字段 `scene_{id}`）；同 `scene_id` 分镜注入场景参考图
- `style_preamble`：全片统一视觉风格，注入 still/motion prompt
- `character_positions`：多角色 left/center/right 站位 + 防串脸文案
- 成片拼接默认 `mute_output=True` 移除音轨
- 未做：BGM、后台 submit+poll、Agnes seed（API 未支持）

### 测试

- P3 新增 7 项单测；流水线相关共 19 项通过
