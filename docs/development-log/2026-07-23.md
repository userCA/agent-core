# 2026-07-23

## Skill 层 GRPO 理念进化（P0–P2）

- **类型/采集**：`PathStep`；`SkillEvolutionTrace` 增加 `steps/group_id/task_key/reward/advantage/human_signal`；Collector 从 `ToolExecutionStart/End` 重建路径；JSONL store roundtrip
- **打分**：`HybridReward`（启发式 + 可选 Judge + 人工，缺省 renormalize）；`build_groups` + `assign_advantages` / `score_group`
- **蒸馏/门控**：`distill_group` → Path Preference + `add_case`；`evaluate_acceptance_gates`（\(\Delta\bar{r}\)、关键回归、步数恶化）；`OfflineEvolutionAgent._generate_path_proposals` 接入
- **文档**：`docs/superpowers/specs/2026-07-23-skill-grpo-evolution-design.md`、`docs/superpowers/plans/2026-07-23-skill-grpo-evolution.md`；更新 `docs/skill-self-evolution.md`
- **验证**：`pytest tests/skill_evolution/` → 88 passed（P0–P2 提交时）

## Skill GRPO P3–P4

- **P3 GroupRollout**：`group_rollout.py`；Collector `set_rollout_context`；Scene `ENABLE_GROUP_ROLLOUT`（默认 off）、`GROUP_ROLLOUT_G`
- **P4 Cases**：`cases.py` 读写 `cases/{positive,negative}.jsonl`；`apply_proposal(add_case)` 落盘；`SkillCaseRecallExtension` + `ENABLE_SKILL_CASE_RECALL`（默认 off）；http_sse/h5 ChatAssistant 可选挂载；Builder 支持 `path_cases_section`
- **验证**：`pytest tests/skill_evolution/` → 96 passed
