# 2026-06-15 开发日志

## 本日工作：H5 前端组件标准化迁移 (Phase 1 + 1.5 + Phase 2)

### 变更类型：重构 / 标准化

### 摘要

用 Radix UI headless primitives + lucide-react 系统性地替换手写 UI 组件，消除 5 种折叠实现、手写 Icon/Toast/ConfirmDialog，以及 toggle/switch/tooltip/tabs/side-drawer 的手动实现。

### Phase 1 + 1.5: 折叠/弹窗/提示/图标

| 变更 | 文件 | 说明 |
|------|------|------|
| 新增依赖 | `package.json` | + `@radix-ui/react-collapsible` `@radix-ui/react-alert-dialog` `@radix-ui/react-toast` `lucide-react` |
| 新增 Radix 样式层 | `src/theme/radix-overrides.css` | z-index 归一化、focus-visible、data-state 动画、lucide-spin、collapsible/toast/dialog 动画 |
| 新增折叠封装 | `src/components/shared/CollapsibleSection.tsx` | 薄封装 `@radix-ui/react-collapsible`，共享 `.collapsible-content` 动画 |
| 折叠统一 | `ChannelsPage.tsx` `ConnectorsPage.tsx` `ExpertsPage.tsx` `KnowledgePage.tsx` `SkillsPage.tsx` `EvolutionPanel.tsx` | 5 个 set-toggle + 1 个 boolean 折叠统一为 Radix Collapsible，删除手写 aria-expanded |
| ConfirmDialog | `ConfirmDialog.tsx` `.css` | 用 `@radix-ui/react-alert-dialog` 替换，保留 `useConfirmStore` API |
| Toast | `Toast.tsx` | 用 `@radix-ui/react-toast` 替换，使用 `Toast.Provider`/`Viewport` |
| Icon | `Icon.tsx` | 用 `lucide-react` 组件映射 53 个 name，保留 `ICON_SIZES` 和 props API |
| 主入口 | `main.tsx` | 追加 `import './theme/radix-overrides.css'` |

### Phase 2: 开关/提示/标签栏/侧栏抽屉

| 变更 | 文件 | 说明 |
|------|------|------|
| 新增依赖 | `package.json` | + `@radix-ui/react-switch` `@radix-ui/react-tooltip` `@radix-ui/react-tabs` `@radix-ui/react-dialog` |
| Switch | `SkillsPage.tsx` `Pages.css` `h5.css` | `.skill-toggle` 按钮 → `Switch.Root`/`Switch.Thumb`，`.on` → `[data-state="checked"]` |
| Tabs | `BottomTabBar.tsx` | 导航 → `Tabs.Root` + `Tabs.Trigger asChild`，保留 `layoutId` motion indicator |
| Dialog | `CompactHeader.tsx` `.css` | 侧栏菜单 → `Dialog.Root`，删除 35 行 useRef/useEffect 手动事件管理，CSS 改用 data-state 动画 |
| Tooltip 包装 | `TooltipWrap.tsx` | 共享组件，透传 radix Tooltip + Portal + Arrow |
| Tooltip 替换 | `ChatInput.tsx` `Header.tsx` `MessageBubble.tsx` `PendingBubbles.tsx` `Sidebar.tsx` `ConnectorsPage.tsx` `SkillsPage.tsx` `ExpertsPage.tsx` `EvolutionPanel.tsx` | 约 22 处 `title=` → `TooltipWrap` |
| Tooltip Provider | `h5/App.tsx` `desktop/App.tsx` | 添加 `<Tooltip.Provider delayDuration={500}>` |
| CSS | `radix-overrides.css` `tokens.css` | 新增 tooltip-content 样式和 data-state 动画，删除废弃 `.tooltip` 伪元素规则（25 行） |

### H5 UI 设计审计修复（10 项）

| 级别 | 修复 | 文件 |
|------|------|------|
| 严重 | 语音模式 300px 面板溢出视口 | `H5ChatInput.css` — `min(300px, 100vw - 32px)` |
| 严重 | 触屏用户复制按钮不可见 | `MessageBubble.css` — 默认 `opacity: 0.6` |
| 严重 | Logo 持续浮动无 reduced-motion | `WelcomeScreen.css` — 动画包裹 `@media (prefers-reduced-motion)` |
| 主要 | 浅色模式助手气泡融入背景 | `h5.css` — 恢复 `var(--surface-soft)` |
| 主要 | 设置/历史页无底部导航 | `BottomTabBar.tsx` — 移除隐藏逻辑 |
| 主要 | 标签指示器 4px 太小 | `BottomTabBar.css` — 改为 6×3px |
| 主要 | 快速标签无触摸反馈 | `ChatInput.css` — 添加 `:active` |
| 主要 | AuthPanel 双重焦点环 | `AuthPanel.css` — 移除冗余 border-color |
| 次要 | 标题省略号无 tooltip | `CompactHeader.tsx` — 添加 `title` 属性 |
| 次要 | 退出登录三层红色 | `SettingsPage.tsx` — 图标改用 var(--mute) |

### 构建验证

```bash
cd scene/http_sse/static && npm run build
# tsc && vite build: 零错误
```
