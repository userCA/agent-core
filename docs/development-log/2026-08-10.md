# 2026-08-10

## feat(workflows): AST 沙箱校验

**类型**：feat（workflows）

**关联**：[`docs/superpowers/plans/2026-08-10-dynamic-workflows.md`](../superpowers/plans/2026-08-10-dynamic-workflows.md) Task 2

### 变更摘要

| 区域 | 文件 | 说明 |
|------|------|------|
| 沙箱 | `agent_core/workflows/sandbox.py`（新） | `validate_workflow_source` AST 校验（禁 import、危险调用、dunder 属性）；`build_sandbox_globals` 注入 ctx/args/安全 builtins/json |
| 测试 | `tests/workflows/test_sandbox.py`（新） | 20 passed |
| 夹具 | `tests/workflows/fixtures/sample_ok.py`、`sample_bad_import.py`（新） | 合法/非法 workflow 样例 |

### 验收

- `pytest tests/workflows/test_sandbox.py -v` → 20 passed

---

## feat(workflows): add workflow asset loader

**类型**：feat（workflows）

**关联**：[`docs/superpowers/plans/2026-08-10-dynamic-workflows.md`](../superpowers/plans/2026-08-10-dynamic-workflows.md) Task 5

### 变更摘要

| 区域 | 文件 | 说明 |
|------|------|------|
| 加载器 | `agent_core/workflows/loader.py`（新） | `WorkflowSpec` + `WorkflowLoader`：扫描 search_paths 与内置 recipes；AST 提取 meta；`validate_workflow_source` 校验；list 跳过无效并 warning，get 抛 `SandboxError`/`KeyError` |
| 内置目录 | `agent_core/workflows/recipes/__init__.py`（新） | 内置配方包占位 |
| 测试 | `tests/workflows/test_loader.py`（新） | 5 passed（tmp_path、fixture、invalid skip、missing KeyError、list_meta） |

### 验收

- `pytest tests/workflows/test_loader.py -v` → 5 passed

---

## feat(workflows): add WorkflowRunner with checkpoint resume

**类型**：feat（workflows）

**关联**：[`docs/superpowers/plans/2026-08-10-dynamic-workflows.md`](../superpowers/plans/2026-08-10-dynamic-workflows.md) Task 6

### 变更摘要

| 区域 | 文件 | 说明 |
|------|------|------|
| Runner | `agent_core/workflows/runner.py`（新） | `WorkflowRunner`：loader/inline 解析 → 沙箱 exec → `await run(ctx)`；checkpoint 自动保存；resume/abort；进度回调 |
| Runtime | `agent_core/workflows/runtime.py` | 新增 `ctx.args` 只读属性 |
| 测试 | `tests/workflows/test_runner_checkpoint.py`（新） | 5 passed（sample-ok、pipeline、resume、abort、inline 门禁） |
| 夹具 | `tests/workflows/conftest.py`、`fixtures/pipeline_demo.py`、`fixtures/two_phase.py`（新） | 共享 `workflow_env` fixture |

### 验收

- `pytest tests/workflows/test_runner_checkpoint.py -v` → 5 passed
