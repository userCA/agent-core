# 2026-08-14 开发日志

## 每日代码优化(optimize-bot)

- **类型**: refactor
- **关联**: 基于 `8848179`(observability OTel gen_ai 对齐)的后续清理

### 候选点评估

| 候选点 | 价值评审 | 风险评审 | 结论 |
|--------|----------|----------|------|
| user_id 解析逻辑去重 + 私有函数公共化 | 值得做(重复代码、命名坏味道,成本约 4 行) | 低风险可做(无外部破坏面、无循环依赖、测试覆盖足够) | **采纳** |
| `_pending_tool_spans` 空 run_id 键冲突 | 存疑(触发条件苛刻,接近"不会发生的 bug") | 需谨慎(改动面大 4 处;call_ctx 方案与 2026-08-11 已记录决策冲突) | **否决** |

### 变更摘要

| 文件 | 说明 |
|------|------|
| `agent_core/observability.py` | `_resolve_observe_user_id` → `resolve_observe_user_id`(公共),补中文注释 |
| `agent_core/session/harness.py` | 导入与调用同步改名 |
| `agent_core/session/turn_runtime.py` | `build_loop_config` 内联 getattr 解析链改为调用 `resolve_observe_user_id(host)` |

行为完全等价:解析优先级(显式 user_id → `observability_user_id` → `owner`)与之前一致。

### 测试

- `tests/session/test_harness_user_id_observe.py` + `tests/core/test_observability.py`:23 passed
- 全量:`pytest -q` → 797 passed, 9 failed(均为外部服务依赖:openviking 7 + agnes e2e 2,预期)
