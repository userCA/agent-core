# 2026-06-06 开发日志

## 68. Header 三栏重构 + MiniGameHost 多游戏 dock

**类型**: 重构 + 功能 | **涉及文件**: 10+

- `Header.tsx`：grid 2 列布局 (left + actions)，MiniGameHost 移至 header-left 内
- `Header.css`：`.header-companion-shell` 伪元素底线+顶线；`.companion-bubble-speech` position:absolute 浮动不推挤；ResizeObserver 相关变量
- `HeaderCompanion.tsx`：`useLayoutEffect` + `ResizeObserver` 动态计算 `--minigame-chip-left`，使 teaser 锚定在猫+气泡右侧
- `CompanionSprite.tsx`：新增 `variant='header'` 截断为 3 行
- `MiniGameHost.tsx`：支持 3 游戏切换 (`~> fish`/`* catch`/`# tap`)，teaser 按钮 → dock shell → result pill 三段式状态
- `FishingGame.tsx`：重构为 inline chip + pond + tension + log + reward 终端风格
- `FishingGame.css`：全量重写，dock shell/connector/lane/teaser/result-pill 全部 inline-flex
- `CatchGame.tsx`/`TapGame.tsx`（新建）：接物游戏+节奏游戏
- `game-api.ts`/`types.ts`（新建）：API 调用抽离 + 类型定义
- `PROGRESS.md`：全面重写，反映实际完成状态

## 69. UX 审查 + focus-visible 修复

**类型**: UX | **涉及文件**: 1

- `.minigame-teaser:focus-visible` 添加 outline，键盘 Tab 导航可感知聚焦状态
