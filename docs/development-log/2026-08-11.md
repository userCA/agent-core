# 2026-08-11

## feat(observability): Langfuse OTLP phase 1

**类型**：feat

**规则**：观测边界 / scene 层出口（core 不绑 Langfuse SDK）

**关联**：[`docs/superpowers/plans/2026-08-11-langfuse-otlp-phase1.md`](../superpowers/plans/2026-08-11-langfuse-otlp-phase1.md)

### 变更摘要

| 区域 | 文件 | 说明 |
|------|------|------|
| OTEL / Langfuse | `agent_core/observability.py` | `configure_langfuse_otel_from_env`、`build_langfuse_otlp_headers`、OTLP/HTTP exporter、span 属性 alias |
| Harness | `agent_core/session/harness.py` | `_execute_turn` 统一 `run_id` + `observe()` 包裹 |
| Turn runtime | `agent_core/session/turn_runtime.py` | `build_loop_config` 接受并下传 `run_id` |
| Scene | `scene/http_sse/server.py`、`scene/h5/server.py` | lifespan 优先 Langfuse OTLP，否则回退现有 exporter |
| 依赖 | `pyproject.toml` | extras `[otel]`（HTTP + gRPC exporter） |
| 测试 | `tests/core/test_observability.py`、`tests/session/test_harness_observe_run_id.py` | Langfuse 配置、harness run_id、tracing hook 回归 |
| Hook 清理 | `agent_core/session/harness_config.py`、`turn_runtime.py` | `remove_*_tool_call_hook` 真正 unsubscribe |
| 设计 | `docs/superpowers/specs/2026-08-11-langfuse-scene-integration-design.md` | 轨 B 阶段 1 规格 |
| 计划 | `docs/superpowers/plans/2026-08-11-langfuse-otlp-phase1.md` | 实施计划 |

### 审查后修复（同会话）

- **tool span 无法 end**：`tool_runner` after-hook 使用新 dict，不能依赖 before `call_ctx["__tracing"]` → `_pending_tool_spans` 按 `{run_id}:{tool_call_id}` 关联
- **hook 累积**：`remove_before/after_tool_call_hook` 原为 no-op → `register_legacy_*` 返回 unsub，`remove_*` 调用
