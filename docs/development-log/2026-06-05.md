# 2026-06-05 开发日志

## 60. 咪兔伴侣系统 Phase 1-5 完整实现

**类型**: 新功能 | **涉及文件**: 33 个（26 新建 + 7 修改）

### Phase 1 — 登录页伴侣（多帧动画 + 输入响应）
- `CompanionSprite.tsx`：5 moods × 3 frames，500ms idle sequence 驱动 fidget
- `LoginCompanion.tsx`：根据输入状态切换 sleeping/awake/happy/working
- `companion-store.ts`：Zustand store 管理 mood + bones + revealed
- `LoginPage.tsx`：静态 ASCII → LoginCompanion；`LoginPage.css` 移除迁移样式

### Phase 2 — 确定性用户专属伴侣
- `companion/bones.py`：FNV-1a hash + mulberry32 PRNG，roll_companion(uid)
- `companion/species.py`：稀有度/眼睛/耳朵/配饰/调色板枚举（append-only）
- `server.py`：`GET /api/companion/{uid}` 返回确定性 bones
- 前端 rarity 揭示动画，login 点击时调用 API

### Phase 3 — CompanionExtension 集成
- `extensions/companion.py`：实现 Extension Protocol，on_event 过滤 TurnStart/TurnEnd 等
- `server.py`：SSE 流 interleave companion 事件；`chat_assistant.py`：optional companion 参数
- `Header.tsx`：静态 logo → HeaderCompanion（streaming 时 working 帧）

### Phase 4 — 伴侣记忆 + 引路 NPC
- `companion/memory.py`：CompanionMemory（uid 作为 session_id，跨 session 持久化）
- `companion/observer.py`：SilentObserver（话题提取、工具历史、空闲检测）
- `companion/guide.py`：GuideNPC 规则引擎（欢迎/工具建议/超时提醒/随机贴士）
- `companion/templates.py`：BondLevel 四级 + 模板槽位填充（零 LLM）
- `companion/topic_extractor.py`：关键字提取 + 敏感词黑名单 + 长度校验
- 默认 InMemoryMemoryStore，构造注入 MemoryStore 即可切换持久化

### Phase 5 — 钓鱼小游戏（种子+重放）
- `companion/minigames/`：MiniGame Protocol + FishingGame 5 阶段状态机
- `server.py`：`GET /api/minigame/config`（下发签名种子）+ `POST /api/minigame/feed`（验签发奖）
- `FishingGame.tsx`：前端本地 PRNG 跑完整游戏，零延迟交互
- `MiniGameHost.tsx`：streaming 3-15s 显示入口，>15s 自动展开

### 架构审查修复
- 消除 `companion/guide.py` → `extensions/companion.py` 循环导入（抽 `companion/types.py`）
- 收敛 `InMemoryMemoryStore` 创建到 `CompanionExtension` 内部（chat_assistant 不再关心存储）
- 抽 `companion_event_to_sse()` 到 `extensions/companion.py`（server.py 不再含 SSE 格式逻辑）

## 61. 品种/怪癖/帽子系统对齐 + 情绪状态机实现

**类型**: 功能增强 | **涉及文件**: 7 个

- `species.py`：新增 BREED_WEIGHTS/BREED_RARITY_GATE/QUIRKS/QUIRK_BREED_BONUS/HATS
- `bones.py`：CompanionBones 新增 breed/quirk/hat 字段；roll 追加在原始序列之后（PRNG 兼容）
- `state_machine.py`（新建）：6 状态 EmotionFSM，7×7 转换表，品种差异化情绪参数，衰减率
- `extensions/companion.py`：集成 EmotionFSM，SSE 事件新增 emotion/eye_override/frontend_mood
- `server.py`：API 返回新字段；/api/minigame/config 返回 fish_table（消除前端 LOCAL_FISH 重复）
- `docs/companion-design.md`：对齐字段定义，顶部指针指向详细设定集

## 62. 前端品种精灵库 + 边界清理

**类型**: 功能增强 + 重构 | **涉及文件**: 9 个

- `breed-sprites.ts`（新建）：7 品种 × 5 mood × 3 帧，品种视觉差异化
- `CompanionSprite.tsx`：新增 breed/eyeOverride props；eye 字符动态覆盖
- `companion-store.ts`：新增 CompanionEmotion 类型 + setEmotion()；删除 setMood()
- `LoginCompanion.tsx`：登录页 mood 从 local props 派生（无 SSE 连接，例外场景）
- `HeaderCompanion.tsx`：删除本地 mood 逻辑；传递 breed/eyeOverride
- `FishingGame.tsx`：删除 LOCAL_FISH；鱼表从 API params.fish_table 获取
- `types.ts`：新增 CompanionEvent/CompanionBubbleEvent SSE 类型
- `useSSE.ts`：处理 companion/companion_bubble SSE 事件 → setEmotion()
- `extensions/companion.py`：新增后台 asyncio decay loop（每 10s 检测情绪衰减并推送）

### 边界规则文档
- `companion-design/14-frontend-backend-boundary.md`：职责分配、数据源唯一性、例外清单
- `companion-design/PROGRESS.md`：实现进度 + 未实现清单

### 设定集（13 份文档）
- `companion-design/01-13` + README：角色/视觉/动画/性格/稀有度/成长/互动/情绪/日周期/里程碑/命名/品种卡/形象演化
- `companion-design/appendix/sprite-frame-library.md`：完整 ASCII 帧库

## 63. 品种角色卡集成 + LLM 命名

**类型**: 功能增强 | **涉及文件**: 7 个

- `templates.py`：四品种专属问候/贴士/工具观察（橘猫=吃、暹罗=八卦、黑猫=谜语、奶牛=亢奋）；`breed_talkativeness()` 语速调节
- `guide.py`：GuideNPC 接受 breed 参数；气泡频率按品种 talkativeness 缩放（暹罗 0.9→频繁，黑猫 0.2→沉默）；品种专属工具观察
- `extensions/companion.py`：品种专属错误安慰气泡；传递 breed 给 GuideNPC
- `naming.py`（新建）：hatch_name() 通过 agent_core ModelProvider 调 LLM 生成 name+personality；`configure_naming()` 模块级注入 provider；名池降级
- `server.py`：`POST /api/companion/{uid}/hatch` 端点；lifespan 中一次性注入 provider 配置（走 MINIMAX_API_KEY）
- `companion-store.ts`：reveal() 改为先调 hatch；新增 name/personality/hatched_at 字段
- `LoginCompanion.tsx`：揭示时显示 "名字 · 品种"

## 64. 聊天气泡渲染 + 位置修复

**类型**: 功能 + bug fix | **涉及文件**: 4 个

- `companion-store.ts`：新增 bubble 状态 + setBubble()
- `useSSE.ts`：companion_bubble 事件 → store.setBubble()
- `HeaderCompanion.tsx`：猫右侧渲染气泡，auto-dismiss (ttl_ms)
- `Header.css`：气泡样式匹配设计系统（--surface-soft/--hairline/--mute/--font-mono/--radius-sm），三角指向左侧猫
