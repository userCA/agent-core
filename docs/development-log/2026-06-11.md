# 2026-06-11 开发日志

## 问题 78. H5 UI/UX Round 2 — 触控优化与交互细节

**涉及文件：**
- `scene/http_sse/static/src/h5/components/AuthPanel.css` — 移除 label 无意义的 `text-transform: uppercase`
- `scene/http_sse/static/src/h5/components/BottomTabBar.css` — 底部安全区左右 padding 适配 + 补充 tab indicator 样式
- `scene/http_sse/static/src/h5/components/CollapsibleInput.css` — 关闭按钮增加 `-webkit-tap-highlight-color: transparent`
- `scene/http_sse/static/src/h5/components/CompactHeader.css` — 菜单按钮/关闭按钮增加 tap-highlight；关闭按钮 touch target 36px→40px
- `scene/http_sse/static/src/h5/components/CompactHeader.tsx` — 菜单项 stagger delay 优化（`index * 0.05` → `0.02 + index * 0.03`）
- `scene/http_sse/static/src/h5/components/H5ChatInput.css` — 上传文件图标、文件标签、语音/文本切换按钮增加 tap-highlight
- `scene/http_sse/static/src/h5/components/LoginPage.css` — 输入框、登录按钮、最近使用列表项增加 tap-highlight

**变更类型：** UI/UX 修复与优化

**关联规则：** dev-process-h5（移动端触控与视觉优化）

### 修复清单

| # | 优先级 | 问题 | 修复方案 |
|---|--------|------|---------|
| 1 | P1 | 中文 label 被大写处理 | 移除 `text-transform: uppercase`，保留 `letter-spacing: 0.04em` |
| 2 | P2 | 底部标签栏缺少左右安全区适配 | `padding-left/right: env(safe-area-inset-*)` |
| 3 | P2 | 底部标签栏缺少 active indicator | 补充 `.h5-tab-indicator` 圆点样式 |
| 4 | P2 | 多处可点击元素有点击高亮残留 | 统一添加 `-webkit-tap-highlight-color: transparent` |
| 5 | P2 | 关闭按钮 touch target 不足 | `min-height/width: 36px` → `40px` |
| 6 | P3 | 菜单项展开 stagger 延迟过长 | `index * 0.05` → `0.02 + index * 0.03` |

### 验证

- `npm run build` 通过，无 TypeScript/Vite 错误
- 构建时间 1.43s，产出 6 个资源文件

---

## 问题 79. H5 UI/UX Round 3 — 无障碍性与细节修复

**涉及文件：**
- `scene/http_sse/static/src/h5/components/SettingsPage.css` — 移除卡片标题 `text-transform: uppercase`
- `scene/http_sse/static/src/h5/components/HistoryPage.css` — 移除日期分组标题 `text-transform: uppercase`
- `scene/http_sse/static/src/h5/components/AuthPanel.css` — focus ring `box-shadow` → `outline + outline-offset`
- `scene/http_sse/static/src/h5/components/H5ChatInput.tsx` — checkbox 增加 `aria-label`
- `scene/http_sse/static/src/h5/components/CollapsibleInput.tsx` — FAB 和关闭按钮增加 `aria-label`
- `scene/http_sse/static/src/h5/components/LoginPage.tsx` — input 和 button 增加 `aria-label`
- `scene/http_sse/static/src/h5/components/H5ChatInput.css` — `text-light` 渐变文字动画增加 `prefers-reduced-motion` 支持

**变更类型：** UI/UX 修复与优化

**关联规则：** dev-process-h5（移动端触控与视觉优化）

### 修复清单

| # | 优先级 | 问题 | 修复方案 |
|---|--------|------|---------|
| 1 | P2 | SettingsPage 卡片标题 uppercase | 移除 `text-transform: uppercase` |
| 2 | P2 | HistoryPage 日期标题 uppercase | 移除 `text-transform: uppercase` |
| 3 | P2 | AuthPanel focus ring 溢出 | `box-shadow` → `outline: 2px solid var(--accent); outline-offset: 2px` |
| 4 | P2 | 多处可交互元素缺少 aria-label | 为 checkbox、FAB、dismiss、input、button 添加 aria-label |
| 5 | P3 | text-light 渐变文字动画无障碍缺失 | `@media (prefers-reduced-motion: reduce)` 禁用动画，降级为纯色 |

### 验证

- `npm run build` 通过，无 TypeScript/Vite 错误
- 构建时间 1.04s，产出 6 个资源文件

---

## 问题 80. H5 UI/UX Round 4-8 — 持续修复

**涉及文件：**
- `scene/http_sse/static/src/h5/components/CompactHeader.css` — header actions/menu 按钮 touch target 24px→36px
- `scene/http_sse/static/src/h5/theme/h5.css` — 全局 `.btn` 和表单元素增加 `tap-highlight`
- `scene/http_sse/static/src/h5/components/H5ChatInput.css` — 移除未定义的 `circle1` keyframes 引用
- `scene/http_sse/static/src/h5/components/BottomTabBar.css` — `transition: all` → 具体属性
- `scene/http_sse/static/src/h5/components/H5ChatInput.css` — 6 处 `transition: all` → 具体属性
- `scene/http_sse/static/src/h5/components/HistoryPage.css` — 搜索输入框/清除按钮 focus 样式 + touch target
- `scene/http_sse/static/src/h5/components/H5ChatInput.css` — `.input-text` focus 样式

**变更类型：** UI/UX 修复与优化

**关联规则：** dev-process-h5（移动端触控与视觉优化）

### 修复清单

| # | 优先级 | 问题 | 修复方案 |
|---|--------|------|---------|
| 1 | P2 | Header actions 按钮 touch target 不足 | `min-height/width: 24px` → `36px` |
| 2 | P2 | 全局按钮和表单元素缺少 tap-highlight | 添加 `-webkit-tap-highlight-color: transparent` |
| 3 | P2 | 未定义的 `circle1` keyframes | 移除引用 |
| 4 | P2 | `transition: all` 性能开销 | 改为只过渡必要属性 |
| 5 | P3 | HistoryPage/H5ChatInput 输入框 focus 缺失 | 添加 `outline: 2px solid var(--accent)` |
| 6 | P3 | 搜索清除按钮 touch target 不足 | `20px` → `28px` |

### 验证

- `npm run build` 通过，无 TypeScript/Vite 错误
- 构建时间 1.20s，产出 6 个资源文件

---

## 问题 81. HistoryPage 搜索清除按钮图标与样式优化

**涉及文件：**
- `scene/http_sse/static/src/h5/components/HistoryPage.css` — 搜索清除按钮对比度与按下反馈优化
- `scene/http_sse/static/src/components/shared/Icon.tsx` — 补全 `x` 图标定义，新增 `cat`、`heart`、`bar-chart`、`sparkles`、`eye` 图标

**变更类型：** UI/UX 修复与优化

**关联规则：** dev-process-h5（移动端触控与视觉优化）

### 修复清单

| # | 优先级 | 问题 | 修复方案 |
|---|--------|------|---------|
| 1 | P2 | 搜索清除按钮无图标（`name="x"` 未定义） | Icon 组件补充 `x` 图标（与 `close` 同形） |
| 2 | P2 | 搜索清除按钮对比度不足 | 背景 `var(--canvas)` → `var(--hairline)`，图标 `var(--ash)` → `var(--mute)` |
| 3 | P3 | 搜索清除按钮按下反馈不明显 | active 态背景变深 + 图标反白，添加过渡动画 |

### 验证

- `npm run build` 通过，无 TypeScript/Vite 错误
- 构建时间 1.19s，产出 6 个资源文件
