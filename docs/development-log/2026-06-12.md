# 2026-06-12

## 73. skill_evolution 模块全面审查、Bug 修复与三层架构落地

**背景**：`agent_core/skill_evolution/` 模块架构骨架合理，但存在多个关键 bug 且未接入生产流程。

**Bug 修复**（6 个）：
1. `validation.py:231` — `_apply_proposal` modify 路径 `target_rule.split('_')[1]` 在中文格式"规则 14"下 IndexError，新增 `_extract_rule_number` 统一处理
2. `agent.py:390` — `_resolve_conflicts` 分母错误：`score / best.confidence` 应为 `score / best_score`
3. `agent.py:216` — `_analyze_success_trace` 遍历 `new_rules_discovered` 遇到第一条就 return，改为 list 收集全部
4. `collector.py:228` — `record_user_feedback` 的 `was_helpful=None` 默认标记 FAILURE，改为 PARTIAL
5. `agent.py:256,259` — `exception_type` 提取后未使用、`proposals = []` 死变量，已删除
6. `collector.py:77-85 + 148-155` — 用户查询提取逻辑重复，抽取 `_extract_user_query` 静态方法

**三层落地实现**：
- **Layer 1**：`collector.py` 重写，基于 `Extension.on_event` 自驱动——监听 `AgentStart`/`TurnEnd`，从 `ctx.agent.state.system_prompt` 解析 `<skill>` 标签，从 `message.error_message` 判定失败。零核心文件改动
- **Layer 2**：`agent.py` 接入 `model_provider`，新增 `_call_llm` / `_llm_analyze`，LLM 可用时驱动分析，不可用时回退启发式
- **Layer 3**：`validation.py` 新增 `agent_runner` 回调参数，可用时真实 agent 执行 test case

**文件变更**：
- `agent_core/skill_evolution/collector.py`：基于 on_event 重写
- `agent_core/skill_evolution/agent.py`：LLM 驱动分析 + 批量分析 + trace 去重 + 成本控制
- `agent_core/skill_evolution/validation.py`：agent_runner 注入 + 人审门控 + diff_proposal
- `agent_core/skill_evolution/audit.py`：审计日志工具（新增）
- `agent_core/skill_evolution/__init__.py`：导出 audit 函数
- `scene/http_sse/server.py`：7 个 `/skills/evolution/` API 端点
- `scene/http_sse/static/src/api/client.ts`：6 个 evolution API 函数
- `scene/http_sse/static/src/stores/skill-store.ts`：evolution 状态管理
- `scene/http_sse/static/src/components/pages/EvolutionPanel.tsx`：进化管理面板（新增）
- `scene/http_sse/static/src/components/pages/Pages.css`：进化面板样式
- `scene/http_sse/static/src/components/pages/SkillsPage.tsx`：集成 EvolutionPanel
- `scene/http_sse/static/src/main.tsx`：`?mode=desktop` / `?mode=h5` 切换
- `CLAUDE.md`：更新本地验证 & 前端开发说明
- `tests/skill_evolution/test_agent.py`：批量分析 + 去重测试
- `tests/skill_evolution/test_integration.py`：on_event 采集 + 人审门控 + diff 测试

## 74. EvolutionPanel 前端 UI 设计

**设计方向**：Precision Lab（精密实验室）— 暗色基调 + 噪点/扫描线纹理 + 生物荧光色标

**功能**：
- Trace 统计展示（数值 easing 动画）
- 分析触发按钮（shimmer 动效）
- Proposal 列表：操作标签、SVG 置信度光环（glow 滤镜）、Diff 终端语法高亮（+/- 行着色、变更统计）
- Accept/Reject 操作 + 审计轨迹

**文件变更**：
- `scene/http_sse/static/src/components/pages/EvolutionPanel.tsx`：完整组件
- `scene/http_sse/static/src/components/pages/Pages.css`：全套暗色主题样式
