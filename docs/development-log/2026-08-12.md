# 2026-08-12

## Skill 严格渐进式暴露（阶段 A + B1）

- **类型**: feature
- **关联方案**: `docs/superpowers/specs/2026-08-12-skill-progressive-disclosure-design.md`

### 变更摘要

| 文件 | 说明 |
|------|------|
| `agent_core/resources/skill_activation.py` | 激活追踪、frontmatter 剥离、`/skill:` 展开 |
| `agent_core/tools/load_skill.py` | 新增 `load_skill` 工具 |
| `agent_core/prompts/builder.py` | skills 段增加渐进式使用指引 |
| `agent_core/session/harness.py` | `record_skill_activation` / run 级激活记录 |
| `agent_core/observability.py` | run span 写入 `agent.skills.activated/sources` |
| `agent_core/skill_evolution/collector.py` | 默认关闭 tool→skill 主归因（`AGENT_SKILL_LEGACY_TOOL_MAP` 可回退） |
| `scene/skill_runtime.py` | scene 共享 SkillRuntime |
| `scene/h5|http_sse|cli/chat_assistant.py` | 注册 load_skill、统一激活与 SkillStart/End |
| tests | load_skill、activation、builder、collector、observability |

### 开关

- `AGENT_SKILL_PROGRESSIVE=1`（默认）：启用 load_skill + prompt 指引
- `AGENT_SKILL_LEGACY_TOOL_MAP=0`（默认）：tool 调用不再自动激活 skill

### 阶段 C（2026-08-12 续）

- `AGENT_SKILL_PATH_READ_ACTIVATION=1`：读 `SKILL.md` 路径时视同 skill 激活（`path_read`）
- H5/http_sse SkillsPage 增加 `load_skill` / `/skill:` 使用说明
- `skill_mapping` 持久化标注 `attribution: display_only`（仅供历史 UI，非归因真相源）

### H5 前端修复

| 文件 | 说明 |
|------|------|
| `scene/h5/static/src/stores/model-store.ts` | 默认模型改为 agnes/agnes-2.0-flash，避免 loadModels 前误用 openai |
| `scene/h5/static/src/hooks/useSSE.ts` | 认证/缺 key 错误映射到 message.error；移除 message.end 泛化「服务不可用」 |

## OTEL GenAI 属性对齐（Langfuse Dashboard）

- **类型**: feature
- **关联方案**: `docs/superpowers/plans/2026-08-12-langfuse-genai-dashboard-attributes.md`

### 变更摘要

| 文件 | 说明 |
|------|------|
| `agent_core/observability.py` | `trace_llm_call` 改写 `gen_ai.*`；删 `llm.*`/`latency_ms`；`observe()` + tool hook 传播 `user.id` |
| `agent_core/core/context.py` | `AgentLoopConfig.user_id` |
| `agent_core/session/harness.py` | `observability_user_id` 属性 |
| `agent_core/core/loop.py` | llm span 传入 `user_id` |
| `scene/h5|http_sse/chat_assistant.py` | `owner`/`uid` → `harness.observability_user_id` |
| `tests/core/test_observability.py` | GenAI 属性断言 |
| `tests/session/test_harness_user_id_observe.py` | user_id 传播到 loop config |
